# Core dependencies
sentence-transformers>=2.2.0
scikit-learn>=1.0.0
numpy>=1.21.0
nltk>=3.8.0
fuzzywuzzy>=0.18.0
python-Levenshtein>=0.12.0  # For better fuzzy matching performance
redis>=4.0.0
tiktoken>=0.5.0  # For accurate token counting

# Async support
asyncio>=3.4.3

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0
pytest-benchmark>=4.0.0
pytest-timeout>=2.1.0
pytest-xdist>=3.0.0  # For parallel test execution
memory-profiler>=0.61.0
psutil>=5.9.0

# MCP dependencies
mcp>=0.1.0  # Model Context Protocol SDK

# Optional performance dependencies
faiss-cpu>=1.7.0  # For faster vector search (optional)
annoy>=1.17.0  # Alternative vector index (optional)

# Optional visualization dependencies  
matplotlib>=3.5.0  # For benchmark plots (optional)