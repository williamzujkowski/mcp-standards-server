apiVersion: 1

providers:
  - name: MCP Standards Server Dashboards
    orgId: 1
    folder: MCP Standards Server
    folderUid: mcp-standards
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
